mixin search(keywords)
    form(id="form-search")
        .input-group 
            input(
                type="text"
                placeholder="Nhập từ khóa" 
                name="keyword"
                class="form-control"
                value=keywords
                )
            .input-group-append
                button(
                    class="btn btn-success"
                    type="submit"
                    ) Tìm